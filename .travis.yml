language: php
php:
  - 5.6
  - 5.5
  - 5.4
  - hhvm
  - hhvm-nightly

matrix:
  allow_failures:
    - php: hhvm-nightly

before_script:
  - composer update --dev --no-interaction --prefer-dist

script:
  - ./vendor/phpunit/phpunit/phpunit
